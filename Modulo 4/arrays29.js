/*
* 
* Arrays
* Principais Métodos do Objeto Array
*
*/

/*
 * concat(arg1, arg2, ..., argn);
 * 
 * indexOf(elementoProcurado[, indice])
 * 
 * lastindexOf(elementoProcurado[, indice])
 * 
 * Join([arg])
 * 
 * pop()
 * 
 * push()
 * 
 * reverse()
 * 
 * shift()
 * 
 * slice(inicio, fim)
 * 
 * sort()
 * 
 * splice(indice, [arg1, arg2...argn])
 * 
 * toString()
 * 
 * unshift(arg)
 * 
 * // Função callback faz a filtragem
 * 
 * every(funcao(elemento, indice, objeto)[,thisObject])
 * 
 * filter(funcao(elemento, indice, objeto)[,thisObject])
 * 
 * forEach(funcao(elemento, indice, objeto)[,thisObject])
 * 
 * some(funcao(elemento, indice, objeto)[,thisObject])
 * 
 * map(funcao(elemento, indice, objeto)[,thisObject])
 * 
 */
