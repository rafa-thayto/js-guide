/*
* 
* Arrays
* Principais Métodos do Objeto Array
*
*/

var vetor = [1, 2, 'a', 3, 4, 5, 'Rafael', 'a', 777];
var resultado1, resultado2;
// Concatena o array
// var resultado = vetor.concat(4,5,6);

// Retorna a posição do elemento que você está procurando
// caso não encontre retorna -1
// var resultado1 = vetor.indexOf('a');
// var resultado2 = vetor.lastIndexOf('a');

/* Pega o array, transforma ele em uma string e você pode
colocar o separador
resultado1 = vetor.join('|"---"|'); */

// Deleta o último elemento do array e retorna ele
// resultado1 = vetor.pop();
// Deleta o primeiro elemeneto do array e retorna ele
// resultado1 = vetor.shift();

console.log(vetor);
// Push acrescenta elementos no final do array e retorna a quantidade de elementos do vetor
// resultado1 = vetor.push(9, 10, 11);
// Unshift arescenta elementos no começo do array e retorna aquantidade de elementos do vetor
// resultado1 = vetor.unshift(9, 10, 11);
console.log(vetor);
console.log(resultado1);
// console.log(resultado2);

