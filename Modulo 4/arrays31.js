/*
* 
* Arrays
* Principais Métodos do Objeto Array
*
*/

// var vetor = [1, 2, 'a', 3, 4, 5, 'Rafael', 'a', 777];
var vetor = [2, 4, 6, 7 ,8, 10];
var resultado1, resultado2;

var par = function(elemento, indice, objeto) {
    return elemento % 2 == 0;
}

console.log(vetor);

/*--------------------------------------------------------------*/
// Every percorre o vetor e verifica se todos os elementos obedecem
// o critério estabelecido
// resultado1 = vetor.every(par);
/*--------------------------------------------------------------*/
// Transforma o vetor em uma string
// resultado1 = vetor.toString();
/*--------------------------------------------------------------*/
// slice cria um novo array a partir da posição que você indicar atá a posição que você quiser
// resultado1 = vetor.slice(2, 6);
/*--------------------------------------------------------------*/
// Reverse inverte a ordem do array
// resultado1 = vetor.reverse();
// console.log(vetor);
console.log(resultado1);
// console.log(resultado2);

